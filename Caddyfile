toolsforfrontend.com {
  # Serve static files from client build
  root * /var/www/tools-for-frontend/client/dist

  # Reverse proxy API requests to Node.js server
  handle /api/* {
    reverse_proxy localhost:3001
  }

  # Handle placeholder route (maps to /api/image/placeholder)
  handle /placeholder/* {
    uri strip_prefix /placeholder
    reverse_proxy localhost:3001 {
      header_up X-Forwarded-Path /api/image/placeholder
    }
  }

  # Serve static files and handle SPA routing
  handle {
    try_files {path} /index.html
    file_server
  }

  # Enable compression
  encode gzip zstd

  # Security headers
  header {
    # Enable HSTS
    Strict-Transport-Security "max-age=31536000;"
    # Prevent clickjacking
    X-Frame-Options "SAMEORIGIN"
    # XSS Protection
    X-Content-Type-Options "nosniff"
    # Referrer Policy
    Referrer-Policy "strict-origin-when-cross-origin"
  }
}